/*let livia = ["sabonte", "shampoo", "condicionador", "pasta de dente", "escova de dente"]
let pedro = ["chocolate", "salgadinho", "pizza", "bolo", "marshmallow"]
let maria = ["algodão", "remédios", "pomada", "curativo", "soro"]
let alice = ["poker", "uno", "bola", "tabuleiro", "cartas"]

let seção = ["higiene", "alimentação", "cuidados médicos", "lazer"]

let array = [livia, pedro, maria, alice]
let novoArray= []
for (let i=0; i<array.length; i++) {
    for (let j=0; j<array[i].length; j++) {
        novoArray.push(array[i][j])

    }
}
console.log(novoArray)*/

/*const nome = "Ricardo";
console.log(nome)
console.log(nome.substring(0, 1))*/

/*function abbrevName(name){
    let separar= name.split(" ")
    let inicial = (separar[0].charAt(0)) + "." + (separar[1].charAt(0))
    console.log(inicial)
}
abbrevName("Lívia Oliveira")*/

function removeEveryOther(arr){
    let separar = arr.split(" ")
   for (let i=0; i<separar.length; i++) {

   }
  }
  removeEveryOther("isso, aquilo, isso, aquilo")

let nome = "ronaldo"
let cortar = nome.slice(1,-1)
console.log(cortar)